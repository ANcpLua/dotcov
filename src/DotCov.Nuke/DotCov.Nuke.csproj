<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>DotCov.Nuke</RootNamespace>
    <Description>NUKE build component that merges Cobertura reports, writes a job summary, and fails the build below your line and branch coverage thresholds. Add ICoverageReport and go.</Description>
    <PackageTags>$(PackageTags);ci;nuke;build</PackageTags>
    <!-- Nuke 10.1.0 (latest) pulls vulnerable transitives (NuGet.Packaging 6.12.1, System.Security.Cryptography.Xml 9.0.0). Audit only direct refs until upstream Nuke bumps them. -->
    <NuGetAuditMode>direct</NuGetAuditMode>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\DotCov\DotCov.csproj" />
    <PackageReference Include="Nuke.Common" />
    <PackageReference Include="Nuke.Components" />
  </ItemGroup>

</Project>
